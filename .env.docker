# Used by `npm run build:docker` (vite build --mode docker).
# Empty API base URL: the app is served by the API itself, so all calls go to
# the same server as relative /api/... URLs.
VITE_API_BASE_URL=
